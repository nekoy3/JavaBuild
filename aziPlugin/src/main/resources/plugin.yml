name: AziPlugin
version: ${project.version}
main: neko.aziplugin.AziServer
api-version: 1.15

commands:
  ranking:
    description: This command is get ranking.
    usage: /<command>
    default: true
  ping:
    description: This command is gets a ping.
    usage: /<command>
    default: true